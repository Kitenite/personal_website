const ScrollPanel = props => (
  <div>
    {props.children}
    <style jsx>{`
        font-weight : lighter;
        min-height: 100vh;
        height: auto;
        font-size: 30px;
        font-size: 4vh;
        padding-left: 2vw;
        a{

        }
      `}</style>
  </div>
)

export default ScrollPanel
